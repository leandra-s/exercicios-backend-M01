const disjuntores = [false, false, true, false, false, true, false, false];

for (let numDijustor = 0; numDijustor < disjuntores.length; numDijustor++) {
    if (disjuntores[numDijustor] === true) {
        console.log(numDijustor);

    }
}
